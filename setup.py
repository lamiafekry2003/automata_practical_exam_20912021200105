from setuptools import setup, find_packages

setup(
    name="my_automata",
    version="1.0.0",
    description="Implementation of DFA and Turing Machine",
    author="lamia fekry",
    packages=find_packages(),
    install_requires=[],
)